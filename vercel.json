{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/cpproxy/:path*",
      "destination": "/api/cpproxy/:path*"
    }
  ]
}
